mutation updateTask($taskId: String, $name: String, $description: String) {
     updateTask(_id: $taskId, task: { name: $name, description: $description }) {
         _id
         name
         description
         createdDate
         status
     }
 }